export type Student = {
  id: number;
  displayName: string;
};

export type Challenge = {
  id: string;
  value: boolean;
};

export type Skill = {
  id: string;
  value: string;
};

export type Option = {
  id: string,
  name: string
}